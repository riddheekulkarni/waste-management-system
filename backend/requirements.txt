flask
flask-sqlalchemy
werkzeug
pillow
gunicorn
pytest
psycopg2-binary

# YOLOv8 inference — required when WASTE_MODEL_WEIGHTS is set.
# See train_waste_yolov8.ipynb (project root) to produce best.pt.
# The app runs in mock-detection mode without a trained model.
ultralytics>=8.0,<9
